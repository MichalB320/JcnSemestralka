<UserControl x:Class="JCNSemestralka.ButtomSource"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JCNSemestralka"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Button Name="tlacidlo" Grid.Column="0" Background="#FFD2E2ED" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Click="OnClick" MouseEnter="OnEnter" MouseLeave="OnLeave">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Name="nazovX" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">ahoj</TextBlock>
                <Image Height="40" Width="40" Name="obr" Grid.Column="1" Source="/Model/delete-985-512.png" />
            </Grid>
        </Button>
    </Grid>
</UserControl>
